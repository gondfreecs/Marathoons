<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@android:color/holo_red_dark</item>
    </style>

    <style name="StyleText" parent="@android:style/TextAppearance.DeviceDefault.SearchResult.Title">


        <item name="android:fontFamily">@font/pacificotest</item>
        <item name= "android:shadowColor">@android:color/black</item>
       <item name="android:shadowDx">-2</item>
        <item name="android:shadowDy">-2</item>
        <item name="android:shadowRadius">1</item>

    </style>

    <style name="CustomProgressBarHorizontal" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

    <style name="marathoonstyle" parent="android:Theme.Holo.Light" />

</resources>
